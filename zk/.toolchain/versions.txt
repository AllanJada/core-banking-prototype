Pinned for this project. Both extracted into .toolchain/toolbin/ rather than installed
globally, so the version is per-repo and no shell profile is modified.

nargo  1.0.0-rc.3   noir-x86_64-unknown-linux-gnu.tar.gz
                    https://github.com/noir-lang/noir/releases/tag/v1.0.0-rc.3
bb     5.2.0        barretenberg-amd64-linux.tar.gz
                    https://github.com/AztecProtocol/aztec-packages/releases/tag/v5.2.0
@aztec/bb.js 5.2.0  npm, matched to the bb version deliberately

Use:  export PATH="$PWD/zk/.toolchain/toolbin:$PATH"
